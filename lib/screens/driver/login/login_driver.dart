import 'package:flutter/material.dart';
import 'package:teman_asik/screens/driver/login/components/body.dart';

class LoginDriver extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: LoginDriverBody(),
    );
  }
}