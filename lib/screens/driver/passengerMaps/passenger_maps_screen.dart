import 'package:flutter/material.dart';
import 'package:teman_asik/screens/driver/passengerMaps/components/passenger_maps_body.dart';

class PassengerMapsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: PassengerMapBody(),
    );
  }
}